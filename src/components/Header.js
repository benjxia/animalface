import React from 'react'

const header = (i) => {
    return (
        <header>
            <h1>
                header test {i.title}
            </h1>
        </header>
    )
}

export default header
